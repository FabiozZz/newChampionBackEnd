import React from 'react';
import Chips from "./index";

const ChipsBox = ({data}) => {
    return (
        <>
            {/*{data.map(_=><Chips active={}>{_.name}</Chips>)}*/}
        </>
    );
};

export default ChipsBox;